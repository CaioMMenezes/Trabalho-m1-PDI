#include <opencv2/opencv.hpp>

class mediaAritmetica
{
    public:
        mediaAritmetica();
        ~mediaAritmetica();

    static cv::Mat calcularMedia(const cv::Mat& imagem);

    private:
        
};